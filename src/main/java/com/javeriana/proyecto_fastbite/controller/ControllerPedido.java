package com.javeriana.proyecto_fastbite.controller;
public class ControllerPedido {
    double ivaPorcentaje = 0.19;
    /*
            + solicitarMenuProductos() : List
    + enviarSeleccion(productos: List, cantidades: List) : void
    + calcularTotal(productos: List) : double
    + aplicarIVA(subtotal: double) : double
    + crearPedido(productos: List, cantidades: List, total: double) : Pedido
    + confirmarPedido(idPedido: int) : void
    + enviarPedidoACocina(idPedido: int) : void
    */
}
